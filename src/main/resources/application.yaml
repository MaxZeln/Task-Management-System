spring:

  main:
    banner-mode: off
  datasource:
    url: jdbc:postgresql://localhost:5434/taskmanagementsystem
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always

  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate: ddl-auto=update
