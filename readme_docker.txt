#        to start up docker from this directory

#db only for developing
docker-compose -f docker-compose.yml up -d

#        to stop docker please run

#db only for developing
docker-compose -f docker-compose.yml down -v